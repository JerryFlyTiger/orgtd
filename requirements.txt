flask>=3.0
sqlalchemy>=2.0
psycopg[binary]>=3.1
alembic>=1.13

# 帳號系統
flask-login>=0.6          # session 管理
flask-wtf>=1.2            # 表單與 CSRF token（原本完全沒有 CSRF 防護）
argon2-cffi>=23.1         # 密碼雜湊，OWASP 現行首選
authlib>=1.3              # OAuth 2.0 / OIDC client（Google 登入）
requests>=2.31            # authlib 的 Flask client 需要它做 token 交換
email-validator>=2.1      # 註冊時驗證 email 格式

# 部署
gunicorn>=22.0            # 正式環境 WSGI server，取代 Flask 內建的 Werkzeug
python-dotenv>=1.0        # 從 .env 讀密鑰，避免寫死在程式碼裡

# 測試
pytest>=8.0
